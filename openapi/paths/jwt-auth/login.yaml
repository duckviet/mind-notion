post:
  summary: Authenticate to receive a JWT
  operationId: login
  tags: [Auth]
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: ../../components/schemas/Auth/Req_LoginCredentials.yaml
  responses:
    "200":
      description: Authentication successful
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/Auth/Res_AuthTokens.yaml
    "401":
      $ref: ../../components/responses/Unauthorized.yaml
