server:
  host: localhost
  port: "8080"
  mode: debug

database:
  host: localhost
  port: "5433"
  user: postgres
  password: password
  name: collaborative_editor
  ssl_mode: disable

jwt:
  secret_key: your-super-secret-jwt-key-change-this-in-production
  expires_in: 3600 # 1 hour in seconds

redis:
  host: localhost
  port: "6380"
  password: ""
  db: 0

pinecone:
  api_key: ""
  environment: "" # e.g., us-east-1
  index_name: "mind-notion"
  namespace: "__default__"

cohere:
  api_key: ""
  model: "embed-multilingual-v3.0"

cdn:
  account_id: your-cdn-account-id
  access_key_id: your-cdn-access-key-id
  secret_access_key: your-cdn-secret-access-key
  region: us-west-1
  bucket_name: your-cdn-bucket-name
  public_base_url: ""

collab:
  token_secret: your-collab-token-secret
  token_ttl_minutes: 60
