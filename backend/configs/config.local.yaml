server:
  host: localhost
  port: "8080"
  mode: debug

database:
  host: localhost
  port: "5433"
  user: postgres
  password: password
  name: collaborative_editor
  ssl_mode: disable

jwt:
  secret_key: dev-secret-key-not-for-production
  expires_in: 7200 # 2 hours for development

redis:
  host: localhost
  port: "6380"
  password: ""
  db: 0

cdn:
  account_id: your-cdn-account-id
  access_key_id: your-cdn-access-key-id
  secret_access_key: your-cdn-secret-access-key
  region: us-west-1
  bucket_name: your-cdn-bucket-name
  public_base_url: ""
