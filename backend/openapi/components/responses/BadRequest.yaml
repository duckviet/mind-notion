description: "Bad Request. The request is invalid."
content:
  application/json:
    schema:
      type: object
      properties:
        error:
          type: string
          example: "Invalid request."
