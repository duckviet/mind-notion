type: object
properties:
  username:
    type: string
    example: "janedoe"
  password:
    type: string
    format: password
    example: "anotherstrongpassword"
  email:
    type: string
    format: email
    example: "jane.doe@example.com"
required: [username, password, email]
